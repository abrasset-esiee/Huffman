#include <stdlib.h>
#include "liste.h"

Liste* create_liste(char *text) {
    Liste *l = (Liste*) malloc(sizeof(Liste));
    l->premier = NULL;
    int i = 0;
    while (text[i] != '\0') {
        
    }
    
}